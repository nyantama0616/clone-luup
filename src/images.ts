const base = "/images";

const enum ImagePaths {
    Logo = `${base}/logo.webp`,
    Why0 = `${base}/why-0-pc.webp`,
    Why1 = `${base}/why-1-pc.webp`,
    Why2 = `${base}/why-2-pc.webp`,
}

export default ImagePaths;
